# Normalise line endings to LF in the repository regardless of the committer's
# OS, so a reviewer's checkout is byte-identical to what CI and Prisma saw.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
